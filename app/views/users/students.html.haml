- provide(:title, 'All students')
%h2 All students
%ul.students
  - @students.each do |student|
    %li
      //= gravatar_for student
      = link_to student.first_name + '  ' + student.last_name, submissions_path(student)
= will_paginate @students
