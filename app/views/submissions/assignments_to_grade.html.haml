%h2 Assignments to be graded
%table{:class => "table table-striped table-bordered table-condensed"}
 %thead
  %tr
   %th Student Name
   %th Course
   %th Grade
   %th Date Submitted
   %th Feedback
  %tbody
   - @to_grade.each do |submission|
    %tr
     %td= User.find(submission.user_id).first_name + ' ' + User.find(submission.user_id).last_name
     %td= link_to "#{Course.find(submission.course_id).title}",submission_path(submission)
     %td= submission.grade
     %td= submission.created_at
     %td= submission.feedback
